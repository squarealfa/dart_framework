name: map_mapper_generator_test
description: A starting point for Dart libraries or applications.
environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  # defaults_provider_annotations: ^1.0.0
  defaults_provider_generator:
    path: ../defaults_provider_generator
  decimal: ^1.0.0
  fixnum: ^1.0.0

dev_dependencies:
  build_runner: ^1.10.2
  build_verify: ^2.0.0
  pedantic: ^1.11.0
  test: ^1.16.8
