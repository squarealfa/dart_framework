name: squarealfa_validation_test
description: A starting point for Dart libraries or applications.
environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  squarealfa_validation:
    path: ../validation
  decimal: ^1.0.0
  fixnum: ^1.0.0

dev_dependencies:
  squarealfa_validation_generator:
    path: ../validation_generator
  build_runner: ^1.10.2
  build_verify: ^2.0.0
  pedantic: ^1.11.0
  test: ^1.16.8
