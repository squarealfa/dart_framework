name: entity_adapter_generator
description: A starting point for Dart libraries or applications.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  source_gen: ^1.0.0

  entity_adapter:
    path: ../entity_adapter

  squarealfa_validation_generator: ^1.0.0
   
  proto_generator: ^1.0.0
  map_mapper_generator: ^1.0.0

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
