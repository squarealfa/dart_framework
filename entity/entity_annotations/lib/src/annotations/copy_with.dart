/// Annotation that instructs the code generator to
/// build an extension of the class to which it
/// is applied that adds a copyWith method
class CopyWith {
  const CopyWith();
}

const copyWith = CopyWith();
